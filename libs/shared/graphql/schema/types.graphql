type Chat {
  id: String! # 1
  message: String!
  user: ID!
  bot: Boolean!
  updatedAt: String!
}

input ChatInput {
  message: String!
  user: ID!
  bot: Boolean
}

input ChatEvent {
  id: String! # 1
  message: String!
  user: ID!
  bot: Boolean!
  updatedAt: String!
}

type User {
  id: ID!
  name: String!
}
