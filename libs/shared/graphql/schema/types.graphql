type Chat {
  id: String! # 1
  message: String!
  user: ID!
  bot: Boolean!
}

input ChatInput {
  message: String!
  user: ID!
  bot: Boolean
}

input ChatEvent {
  id: String! # 1
  message: String!
  user: ID!
  bot: Boolean!
}

type User {
  id: ID!
  name: String!
}
